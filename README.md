# emotion-detector
This project implements a Convolutional Neural Network (CNN) to detect and classify human emotions from facial expressions. It leverages the Face Expression Recognition Dataset (kaggle.com in Bing) to train and evaluate the model across multiple emotion categories such as happiness, sadness, anger, surprise, fear, and neutral.
